services:
  jwtgenerator:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      ASPNETCORE_ENVIRONMENT: Development
      PORT: "8080"
      Security__ApiKey: ${X_API_KEY}
